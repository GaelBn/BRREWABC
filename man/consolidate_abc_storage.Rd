% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storedData.R
\name{consolidate_abc_storage}
\alias{consolidate_abc_storage}
\title{Consolidate stored ABC summaries and outputs}
\usage{
consolidate_abc_storage(
  x,
  kind = c("summaries", "outputs"),
  name = NULL,
  generation = NULL,
  mode = c("export", "replace"),
  output_dir = NULL,
  keep_fragments = TRUE,
  compression = "zstd",
  overwrite = FALSE,
  dry_run = FALSE
)
}
\arguments{
\item{x}{an object returned by [abcsmc()] or [abcrejection()], or an
experiment/storage directory.}

\item{kind}{one or both of `"summaries"` and `"outputs"`.}

\item{name}{optional stored object names.}

\item{generation}{optional generation numbers.}

\item{mode}{`"export"` to leave the active manifest unchanged, or
`"replace"` to make consolidated files the active storage layout.}

\item{output_dir}{destination directory. Defaults to `consolidated` for an
export and `compacted` for replacement, below the storage root.}

\item{keep_fragments}{whether source fragments should remain on disk after a
successful replacement.}

\item{compression}{Parquet compression codec.}

\item{overwrite}{whether existing consolidated files may be replaced.}

\item{dry_run}{return the consolidation plan without writing files.}
}
\value{
a data frame describing the consolidation performed or planned.
}
\description{
Consolidates the active Parquet fragments for each selected named object and
generation into one Parquet file. By default this creates a separate export
and leaves the operational fragmented storage unchanged.
}
